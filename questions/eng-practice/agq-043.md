---
id: agq-043
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Elasticsearch
feynman:
  essence: 通过量化、专用引擎和批处理提升计算吞吐。
  analogy: 像把货车加宽（量化）并用高速路（专用引擎），再拼车发货（批处理）。
  first_principle: 如何压榨硬件算力以最大化模型吞吐量？
  key_points:
  - 量化降低模型精度以换取计算速度
  - 使用ONNX等专用推理引擎加速
  - 批量处理充分利用GPU算力
  - 服务启动时预加载模型避免冷启动
memory_points:
- 模型量化：FP16转INT8，精度损失<0.5%，速度提升40%。
- 推理引擎：ONNX Runtime去Python GIL，启用CUDA Graphs加速。
- 批处理策略：静态Batch凑数，动态Batch设超时，最大化GPU利用率。
- 预加载：服务启动时模型进显存，解决首次请求Page Fault卡顿。
---

# 模型推理速度是怎么优化的

**Situation：** 本地部署的模型(用于 embedding、reranking 等)推理速度需要优化.
**Task：** 提升本地模型的推理速度,降低延迟.
**Action：** 
1. 模型量化:
  *   Embedding 模型(BGE-M3)使用 INT8 量化.
  *   **细节**：将模型权重从 FP32/FP16 转为 INT8，利用 Tensor Core 加速 INT8 矩阵运算。校准集用于确定量化缩放因子，确保精度损失 < 0.5%,推理速度提升 40%.

2. 推理引擎优化:
  *   使用 ONNX Runtime 替代 PyTorch 推理(去除了 Python GIL 锁和训练相关开销,速度提升 30%).
  *   **加速后端**：启用 CUDA Graphs 减少 Kernel 启动开销，对稳定形状输入使用 TensorRT 进行进一步优化.

3. 批处理:
  *   **静态批处理**：Embedding 计算支持 `batch_size=64`; Reranking 支持批量推理(一次性对 40 个候选打分).
  *   **动态批处理**：设置 `max_batch_size` 和 `timeout_ms`。推理引擎在超时前积累请求，凑齐一批处理，最大化 GPU 利用率.

4. 模型预加载:
  *   服务启动时预加载模型到 GPU 显存,避免首次请求的冷启动延迟(解决 Page Fault 导致的卡顿).

**推理流程架构图：**
```text
Requests Stream
    │
    ▼
[Dynamic Batching Queue]
    │ (Accumulate or Timeout)
    ▼
[Preprocessing] (Tokenization)
    │
    ▼
[ONNX Runtime / TensorRT]
    │ (INT8 Quantized Weights)
    │
    ▼
[Postprocessing] (Normalization)
    │
    ▼
Response
```

**实战案例：** 在一次 Black Friday 大促中，单纯依靠批处理导致长尾请求等待超时。引入了 In-Flight Batching（连续批处理），允许优先处理已计算完成的序列，解决了因单条长文本阻塞整批请求的问题，P99 延迟降低了 60%。

**代码示例（Python）：**
```python
import onnxruntime as ort
# 配置 ONNX Runtime 优化选项
sess_options = ort.SessionOptions()
sess_options.graph_optimization_level = ort.GraphOptimizationLevel.ORT_ENABLE_ALL
# 启用 CUDA 内存分配优化
execution_provider = [('CUDAExecutionProvider', {'gpu_mem_limit': 4096})]
session = ort.InferenceSession("model.quant.onnx", sess_options, providers=execution_provider)
# 推理时指定 IO 绑定以减少内存拷贝
bindings = session.io_binding()
bindings.bind_input('input', device_type='cuda', device_id=0, element_type=np.float32, shape=input_array.shape, buffer_ptr=input_array.ctypes.data)
```

**对比表格（推理方案选型）：**
| 特性 | PyTorch (Eager) | TorchScript | ONNX Runtime | TensorRT |
| :--- | :--- | :--- | :--- | :--- |
| **易用性** | ⭐⭐⭐⭐⭐ (最高) | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ (需繁琐转换) |
| **推理速度** | ⭐⭐ (基准) | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ (最快) |
| **硬件支持** | 广泛 | NVIDIA/CPU | 广泛(含 ARM/NNPA) | 主要是 NVIDIA |
| **适用场景** | 研发/调试 | 生产环境(简单) | 跨平台/生产首选 | 极致性能/NVIDIA专属 |

**Result：** 
*   Embedding 计算延迟:单条 15ms → 批量 1000 条 3s(均摊 3ms/条).
*   Reranking 延迟:40 条候选 120ms(batch 推理).
*   GPU 利用率从 30% 提升到 75%.

## 常见考点
1.  **KV Cache**：在生成式 LLM 中，KV Cache 是什么？如何优化显存占用（如 PagedAttention）？
2.  **Continuous Batching**：在 LLM 推理中，为什么 Continuous Batching（连续批处理）比 Static Batching 效率更高？（解决“长短不一”的 Pad 问题）。
3.  **AIGC vs 非生成式**：Embedding 和 Rerank 模型的优化与 Chat 模型的主要区别（无 Decode 阶段，主要瓶颈在计算）。

## 记忆要点

- 模型量化：FP16转INT8，精度损失<0.5%，速度提升40%。
- 推理引擎：ONNX Runtime去Python GIL，启用CUDA Graphs加速。
- 批处理策略：静态Batch凑数，动态Batch设超时，最大化GPU利用率。
- 预加载：服务启动时模型进显存，解决首次请求Page Fault卡顿。

