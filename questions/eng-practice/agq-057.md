---
id: agq-057
difficulty: L3
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Redis
- 索引
- RDB
- AOF
feynman:
  essence: 通过数据分级备份策略和定期演练，实现关键数据的持久化和快速恢复。
  analogy: 重要文件存保险柜，普通文件存硬盘，定期演习火灾怎么抢救。
  first_principle: 如何在系统发生灾难性故障时，确保关键数据不丢且能快速恢复业务？
  key_points:
  - 数据重要性分级(RPO)
  - 数据库主从实时同步
  - 定期灾难恢复演练
  - 向量索引可重建机制
memory_points:
- 数据分级备份：核心数据实时同步(RPO=0)，重要数据每小时增量，可重建每日备。
- 多策略组合：数据库主从复制+WAL归档，向量库定期快照+源文档重建。
- 恢复演练：定期模拟故障演练，记录RTO，确保备份真实可用。
- 降级补充：数据丢失时，根据数据等级启用不同粒度的降级服务策略。
---

# 数据丢失和恢复策略是什么

**Situation：** 关键数据(对话历史、知识库索引、用户偏好等)的丢失会严重影响服务质量.

**Task：** 建立完善的数据备份和恢复机制.

**Action：** 
1. **数据分级:**
   - **核心数据(RPO=0):** 知识库原始文档、用户配置 → 实时同步备份.
   - **重要数据(RPO<1h):** 对话历史、向量索引 → 每小时增量备份.
   - **可重建数据(RPO<24h):** 缓存数据、统计报表 → 每日备份.

2. **备份策略:**
   - **PostgreSQL：** 流式复制(主从同步)+ 每日全量备份 + WAL 归档.
   - **Milvus：** 定期 snapshot + 原始文档可重建索引.
   - **Redis：** RDB 快照(每小时)+ AOF 持久化.

3. **降级策略(补充说明):**
   - L0(正常): 所有功能正常运行
   - L1(轻微): 关闭非核心功能(如推荐、个性化)
   - L2(中度): 模型降级(复杂查询也走快速模型)
   - L3(严重): 只返回缓存和FAQ回答,不调用LLM

4. **恢复演练:**
   - 每季度做一次灾难恢复演练.
   - **模拟场景：** 数据库主节点故障、向量库数据损坏、Redis 全部清空.
   - 记录恢复时间(RTO),确保满足 SLA 要求.

5. **实战案例（新增）：**
   - 曾遇到过一次 Milvus 集群 Etcd 数据损坏导致索引不可用的故障。由于我们保存了所有原始 S3 文档，启动了“索引重建脚本”，并在 4 小时内利用 GPU 节点池完成了 2000 万向量数据的重新 Embedding 和导入，业务仅中断了读服务，无数据丢失。

6. **关键代码示例（新增）：**
   ```python
   # 简单的 RPO/RTO 配置类定义
class DataPolicy:
    CRITICAL = {"rpo_sec": 0, "backup": "sync_replication", "storage": "S3_Standard_IA"}
    IMPORTANT = {"rpo_sec": 3600, "backup": "hourly_snapshot", "storage": "S3_Standard"}
    REBUILDABLE = {"rpo_sec": 86400, "backup": "daily", "storage": "S3_Glacier"}

   def check_compliance(data_type, current_backup_time):
       policy = getattr(DataPolicy, data_type.upper())
       latency = time.time() - current_backup_time
       return latency <= policy["rpo_sec"]
   ```

7. **备份策略对比（新增）：**
| 策略 | RPO (数据丢失量) | RTO (恢复时间) | 成本 | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| 冷备 | 每日/每周 | 数小时/数天 | 低 | 历史归档、合规审计 |
| 温备 | 每小时 | 1-2 小时 | 中 | 普通业务数据、日志 |
| 热备 | 秒级/分钟级 | 分钟级 | 高 | 核心交易库、用户配置 |

**Result：** 
- 核心数据零丢失(RPO=0,主从同步).
- 数据库故障恢复时间 < 5 分钟(主从切换).
- **向量库重建时间：** 约 6 小时(从原始文档重新计算 embedding).

**备份与恢复架构图：**
```text
   [应用服务层]
       │  Write
       ▼
┌──────────────┐     Replication      ┌──────────────┐
│   Primary    │─────────────────────▶│    Standby   │
│   (Master)   │  (Sync/Async Stream) │    (Slave)   │
└──────┬───────┘                     └──────┬───────┘
       │                                     │
       │ WAL Log                             │ (Promotion if Master fails)
       ▼                                     ▼
┌──────────────┐                     ┌──────────────┐
│   Backup     │                     │   Disaster   │
│   System     │◀──── Full/Daily ────│   Recovery   │
│ (NAS/S3/GCS) │     Incremental     │   Procedure  │
└──────────────┘                     └──────────────┘

   [向量库数据流]
       │ Document
       ▼
  ┌─────────┐   Embedding   ┌──────────────┐
  │ Object  │──────────────▶│  Milvus/DB   │
  │ Storage │ (原始文件存档) │ (Vector Data)│
  └─────────┘               └──────┬───────┘
                                    │ Snapshot
                                    ▼
                              ┌──────────┐
                              │   Backup │
                              └──────────┘
```

## 常见考点
1. **RPO 与 RTO 的区别**：请解释两者的含义及在你的系统中的具体数值。RPO (Recovery Point Objective) 指恢复点目标（业务能容忍的数据丢失量，如 0 或 1小时）；RTO (Reco

## 记忆要点

- 数据分级备份：核心数据实时同步(RPO=0)，重要数据每小时增量，可重建每日备。
- 多策略组合：数据库主从复制+WAL归档，向量库定期快照+源文档重建。
- 恢复演练：定期模拟故障演练，记录RTO，确保备份真实可用。
- 降级补充：数据丢失时，根据数据等级启用不同粒度的降级服务策略。

