---
id: llm-061
difficulty: L2
category: llm-core
subcategory: 手撕代码
tags:
- IO
- IOC
- Elasticsearch
feynman:
  essence: 按样本特征维度进行标准化，引入可学习缩放与平移参数。
  analogy: 像给每个学生的各科成绩重新打分，先拉平平均分和波动，再由老师决定是否调整难度。
  first_principle: 如何消除样本内部特征分布差异以稳定训练？
  key_points:
  - 计算每个样本的均值和方差（沿特征维度）
  - 使用均值方差进行归一化（零均值单位方差）
  - 通过gamma和beta参数进行仿射变换
memory_points:
- 计算流程：求均值 -> 求方差 -> 归一化 -> 缩放平移
- 维度操作：均值和方差均在最后一个维度计算
- 参数含义：Gamma缩放，Beta平移，恢复模型表达能力
- 混合精度：统计量建议在FP32计算，防止溢出
- 方差估计：使用有偏估计(unbiased=False)保持与TF一致
---

# 手撕Layer Norm

```python
import torch
import torch.nn as nn

class LayerNorm(nn.Module):
    def __init__(self, normalized_shape, eps=1e-5, elementwise_affine=True):
        """
        Args:
            normalized_shape: 输入形状中从末尾开始归一化的维度，例如 [hidden_size] 或 [batch, hidden_size]
            eps: 防止除零的小常数
            elementwise_affine: 是否进行缩放和平移（引入可学习参数 gamma 和 beta）
        """
        super(LayerNorm, self).__init__()
        self.eps = eps
        self.elementwise_affine = elementwise_affine
        if self.elementwise_affine:
            self.gamma = nn.Parameter(torch.ones(normalized_shape))
            self.beta = nn.Parameter(torch.zeros(normalized_shape))

    def forward(self, x):
        # 1. 计算均值：对最后一个维度求均值
        # keepdim=True 保持维度，便于广播
        mean = x.mean(dim=-1, keepdim=True)
        
        # 2. 计算方差：使用有偏估计 (unbiased=False)，与 TensorFlow 保持一致，且更稳定
        var = x.var(dim=-1, keepdim=True, unbiased=False)
        
        # 3. 归一化
        x_normalized = (x - mean) / torch.sqrt(var + self.eps)
        
        # 4. 仿射变换（缩放和平移）
        if self.elementwise_affine:
            x_normalized = self.gamma * x_normalized + self.beta
            
        return x_normalized
```

### 核心流程图

```text
输入 X (Batch, Seq_Len, Hidden)
          │
          ▼
┌───────────────────────┐
│  计算均值 mean (dim=-1) │
└───────────┬───────────┘
            │
            ▼
┌───────────────────────┐
│  计算方差 var (dim=-1) │
└───────────┬───────────┘
            │
            ▼
    ┌───────────────┐
    │ (X - mean)    │
    │ ───────────── │
    │ √(var + eps)  │
    └───────┬───────┘
            │
            ▼
┌───────────────────────┐      (是)
│ elementwise_affine?   ├────────────────┐
└───────────┬───────────┘                │
            │ (否)                       ▼
            │              ┌───────────────────────────────┐
            │              │  Output = γ * x_norm + β      │
            │              └───────────────┬───────────────┘
            ▼                              │
```

### 实战案例
在混合精度训练（FP16）中，LayerNorm 的方差计算容易溢出。实战中通常在 FP32 下计算均值和方差，然后再 Cast 回 FP16 进行后续计算。PyTorch 的 `torch.nn.LayerNorm` 内部已自动处理此逻辑，但手写实现时需注意：`x - mean` 应该在 FP16 下做还是转 FP32？最佳实践是全程保留 FP32 精度计算中间值。

### 关键代码片段 (FP32 Safe)
```python
def forward_fp16_safe(self, x):
    # 混合精度训练建议：在 FP32 下计算统计量
    if x.dtype == torch.float16 or x.dtype == torch.bfloat16:
        mean = x.mean(dim=-1, keepdim=True, dtype=torch.float32)
        var = x.var(dim=-1, keepdim=True, unbiased=False, dtype=torch.float32)
        x_normalized = (x - mean) / torch.sqrt(var + self.eps)
    else:
        # 原有逻辑
        mean = x.mean(dim=-1, keepdim=True)
        var = x.var(dim=-1, keepdim=True, unbiased=False)
        x_normalized = (x - mean) / torch.sqrt(var + self.eps)
    return self.gamma * x_normalized + self.beta
```

## 记忆要点

- 计算流程：求均值 -> 求方差 -> 归一化 -> 缩放平移
- 维度操作：均值和方差均在最后一个维度计算
- 参数含义：Gamma缩放，Beta平移，恢复模型表达能力
- 混合精度：统计量建议在FP32计算，防止溢出
- 方差估计：使用有偏估计(unbiased=False)保持与TF一致

