---
id: llm-073
difficulty: L2
category: llm-core
subcategory: 位置编码
tags:
- IO
- IOC
- Elasticsearch
images:
- llm100_p200_img1.png
- llm100_p201_img1.png
feynman:
  essence: 在Attention分数上减去与距离成正比的惩罚，越远权重越低。
  analogy: 就像人说话，相邻的词关联最强，隔得太远的词虽然有关联但需要打折扣。
  first_principle: 如何在不引入额外位置参数的情况下，让模型感知相对位置并具备处理无限长度序列的能力？
  key_points:
  - 无嵌入：不需要额外的位置向量参数。
  - 线性偏置：惩罚项与相对距离成正比。
  - 外推性：仅依赖相对距离，天然支持长序列。
  - 作用机制：通过Softmax前的减法操作抑制远距离关注。
memory_points:
- ALiBi 不使用位置 Embedding，直接在 Attention Score 上减去与距离成正比的惩罚。
- 偏置公式为 $-m \cdot |i-j|$，距离越远惩罚越大，促使模型关注邻近 Token。
- 基于相对距离，理论上可外推到任意长度，无需插值，不存在索引越界问题。
- 不同 Head 使用不同斜率 $m$，使模型具备多尺度的感受野，BLOOM 模型采用此方案。
---

# ALiBi (Attention with Linear Biases)思路是什么

### 1. ALiBi 是什么
**ALiBi (Attention with Linear Biases)** 是一种不使用嵌入向量的位置编码方法。它通过在 Attention 的计算分数上直接减去一个与距离成正比的惩罚项来实现位置感知。

### 2. 核心思想
*   **无需位置 Embedding：** 去掉了输入层或注意力层的绝对/相对位置向量加法操作。
*   **线性偏置惩罚：** 在计算 $QK^T$ 得到分数矩阵后，对每个位置对 $(i, j)$ 施加一个偏置 $b = -m \cdot |i - j|$（其中 $i$ 是 query 位置，$j$ 是 key 位置，$m$ 是可学习或预设的斜率）。
*   **距离衰减：** 距离越远，减去的值越大（负得越多），导致 Softmax 后的权重越小。这意味着模型被鼓励更多地关注邻近的 Token。

### 3. 为什么具备长度外推能力
*   **基于相对距离：** 无论序列多长，第 $i$ 个 token 和第 $i+10$ 个 token 的相对距离始终是 10，惩罚值固定。模型不需要学习“位置 5000”是什么样的，只需要学习“距离 10”的影响。
*   **无长度限制：** 理论上可以外推到任意长度，只需继续延长距离计算即可。由于没有位置 Embedding，不存在“索引越界”的问题。

### 4. 计算流程图

```text
Standard Attention Flow with ALiBi

Input: Q [s, d], K [s, d], V [s, d]
│
├─ 1. Compute Dot Product Scores
│   Score[i, j] = Q[i] · K[j]^T
│
├─ 2. Add Positional Bias (ALiBi Step)
│   Bias[i, j] = -m * |i - j|
│   Score'[i, j] = Score[i, j] + Bias[i, j]
│   (注意：这里直接修改 Logits，且 Bias 与序列内容无关)
│
├─ 3. Softmax
│   Attn[i, j] = exp(Score'[i, j]) / Sum(exp(Score'[:, j]))
│   (由于 Bias 随距离增大变得更负，远距离 Token 的概率被压低)
│
└─ 4. Weighted Sum
    Output[i] = Sum(Attn[i, j] * V[j])
```

### 5. 优势与细节
*   **训练与推理一致性：** 短序列训练，长序列推理，无需修改模型结构（如插值）。
*   **实现简单：** 仅需修改 Attention Score 的计算逻辑几行代码，不增加参数（如果 $m$ 固定）或极少参数（如果 $m$ 可学习）。
*   **多头差异：** ALiBi 通常在不同的 Attention Head 中使用不同的斜率 $m$（例如呈几何级数递减），使不同的 Head 关注不同范围的信息。

### 6. 实战案例：BLOOM 模型的选择
Hugging Face 的 **BLOOM** 模型系列（最大 176B 参数）选择了 ALiBi 而非 RoPE。原因在于 BLOOM 需要在多种长度的语料上训练且希望推理时能动态适应超长序列。ALiBi 允许他们在训练时使用较短窗口截断，而在推理时直接扩展到更长序列（如 10k+ tokens）而无需微调，验证了其在工业级大模型中的稳定性。

### 7. 代码示例：ALiBi Bias 生成

```python
import torch

def get_alibi_bias(n_heads, seq_len):
    # 生成斜率 m: 1/2, 1/4, 1/8 ...
    slopes = torch.tensor([1 / (2 ** i) for i in range(n_heads)], dtype=torch.float32)
    
    # 生成距离矩阵 |i-j|
    positions = torch.arange(seq_len).unsqueeze(1) - torch.arange(seq_len).unsqueeze(0)
    distance_matrix = torch.abs(positions)
    
    # 计算 Bias: slopes * distance
    # shape: [n_heads, seq_len, seq_len]
    bias = slopes.unsqueeze(1).unsqueeze(1) * distance_matrix.unsqueeze(0)
    
    return -bias  # 注意 ALiBi 是减去惩罚（加负数）
```

### 8. 常见考点
1.  **对比 RoPE**：ALiBi 和 RoPE 都能做长度外推，核心区别是什么？（ALiBi 不修改 Q/K 向量，只修改 Logits；RoPE 修改 Q/K 向量本身。ALiBi 实现更简单，但 RoPE 在当前开源大模型中更为主流）。
2.  **斜率 $m$ 的设定**：多头注意力中，$m$ 是如何设定的？（通常按照 $1 / 2^{8/h}$ 的方式设置，让不同 Head 拥有不同的衰减速度）。
3.  **推理时的 KV Cache**：ALiBi 对 KV Cache 有什么影响？（由于只需计算距离，且是加性偏置，对 KV Cache 非常友好，不需要额外存储复杂的位置信息）。

## 记忆要点

- ALiBi 不使用位置 Embedding，直接在 Attention Score 上减去与距离成正比的惩罚。
- 偏置公式为 $-m \cdot |i-j|$，距离越远惩罚越大，促使模型关注邻近 Token。
- 基于相对距离，理论上可外推到任意长度，无需插值，不存在索引越界问题。
- 不同 Head 使用不同斜率 $m$，使模型具备多尺度的感受野，BLOOM 模型采用此方案。

