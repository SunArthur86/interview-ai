---
id: ai-a-006
difficulty: L2
category: ai-agent
subcategory: Agent架构
tags:
- Elasticsearch
feynman:
  essence: 让AI像人一样看屏幕、动鼠标实现跨软件操作。
  analogy: 像雇个盲人远程操作电脑，只能看截图并用键盘鼠标控制。
  first_principle: 如何让不具备系统接口的通用AI操作任意图形界面软件？
  key_points:
  - 视觉语言模型(VLM)将屏幕截图转为指令
  - 核心挑战是像素级点击的精确度与动态变化
  - Browser Use操作DOM更准，Computer Use通用性更强
  - 需处理长任务中的错误累积与多窗口管理
  - 必须引入人工确认机制防止危险操作
follow_up:
- 如何提高VLM的点击精度?
- DOM方式和截图方式各有什么优劣?
---

# Claude Computer Use和Browser Use的核心技术挑战是什么

- **常见协作模式:**

1. **层级式**
- Manager → Workers(经理分配任务给工人)
- 例:CrewAI的Crew结构

2. **对话式**
- Agent之间自由对话,达成共识
- 例:AutoGen的GroupChat

3. **流水线式**
- A → B → C,每个Agent处理特定步骤
- 例:写作Agent(初稿) → 编辑Agent(润色) → 事实核查Agent

4. **竞争式**
- 多个Agent提出不同方案,投票选出最优
- 提高准确率,但成本高

5. **Swarm(群体智能)**
- 去中心化协调,Agent根据规则自主协作
- OpenAI Swarm框架

- **角色设计原则:**
- **单一职责** - 每个Agent只擅长一件事
- **互补** - 角色之间技能互补
- **明确边界** - 每个Agent的输入输出清晰
- **协调机制** - 谁做决策、谁执行、谁验证

- **补充细节：**
  - **通信开销**：多Agent系统通信占用的Token成本极高，需设计高效的消息传递协议。
  - **共识机制**：对话式协作中，如何避免陷入无休止的争论？（设置发言人或协调者）。

---

### 架构图：多Agent协作模式对比
```text
【层级式 Hierarchy】        【流水线 Pipeline】
      Manager                    Input
        │                         │
    ┌───┴───┐                     ▼
    │       │                  Agent A
  Worker1 Worker2               │
    │       │                   ▼
    └───┬───┘                Agent B
        │                      │
        ▼                      ▼
     Output                 Agent C
                              │
                              ▼
                           Output

【竞争式 Competition】       【Swarm (图网络)】
    Input                     Agent A ◀──── Agent B
      │                         │  ▲    ▲   │
      ▼                         │  │    │   │
   ┌─────┐     Vote            └──┴────┴───┘
   │Ag 1 │ ────▶ Result
   └─────┘
   │Ag 2 │ ────▶
   └─────┘
```

### ## 常见考点
1. **SOP（标准作业程序）与Agent**：将业务流程转化为多Agent协作的设计思路。
2. **通信协议**：Agent间传递的是自然语言还是结构化数据（JSON/Pydantic）？有何优劣？
3. **冲突解决**：当两个Agent决策相反时，系统如何仲裁？
