---
id: agl-005
difficulty: L1
category: ai-agent
subcategory: 大模型基础
feynman:
  essence: Prefill 是计算密集型，Decode 是内存密集型。
  analogy: 一口气读完前言（Prefill） vs 一个字一个字写正文。
  first_principle: 如何优化 LLM 推理时的算力与内存瓶颈？
  key_points:
  - Prefill：并行处理，计算密集
  - Decode：自回归生成，带宽受限
  - KV Cache 优化 Decode 性能
follow_up:
- Prefill 是算力密集、Decode 是访存密集，分别如何优化？
- 为什么 Decode 阶段 GPU 利用率低？
---

# Prefill 和 Decode 阶段特点

大模型推理分为 Prefill（预填充/编码）和 Decode（解码/生成）两个截然不同的阶段，其计算模式、访存特征和性能瓶颈完全不同。

**1. Prefill 阶段**
- **特点**：处理用户输入的 Prompt。
- **计算模式**：高度并行。输入的所有 Token 同时参与 Self-Attention 计算，利用 GPU 的大规模并行计算能力。
- **性能瓶颈**：受限于**算力**。因为计算密度高，GPU 利用率通常很高，延迟主要取决于 Prompt 长度和模型层数。

**2. Decode 阶段**
- **特点**：自回归生成，一次生成一个 Token。
- **计算模式**：串行生成。每生成一个新 Token，都需要将其与之前的所有历史 Key/Value 进行 Attention 计算。
- **性能瓶颈**：受限于**显存带宽**。每一层的计算量很小（单次矩阵向量乘），但需要加载巨大的 KV Cache（所有历史 Token 的 K, V 矩阵）。计算单元往往在等待数据搬运，导致“访存受限”。

```text
推理生命周期

Prefill 阶段 (计算密集)          Decode 阶段 (访存密集)
+-----------------------+       +-----------------------+
| Prompt: [A, B, C]     |       | History: [A, B, C, D] |
|                       |       |                       |
|  A -> Attn(B,C)       |       |  D -> Attn(A..C)      |
|  B -> Attn(A,C)       |       |  结果 -> Token E      |
|  C -> Attn(A,B)       |       |           |           |
|  (并行计算)           |       |           v           |
|           |           |       |  Update KV Cache      |
|           v           |       |           |           |
|  生成首个 Token D     |       |           +---> Loop  |
+-----------------------+       +-----------------------+
```

**KV Cache 的影响**：
- Decode 阶段复用 Prefill 阶段计算并存储的 Key 和 Value 向量，避免每步重复计算历史 Token。
- KV Cache 占用大量显存，限制了 Batch Size 和最大上下文长度。
- 随着 Sequence Length 增加，Attention 的计算复杂度为 $O(N^2)$，但在 Decode 中主要是读取 Cache，带宽压力与 $N$ 成正比。

### 实战深化
- **实战案例**：在使用 vLLM 部署服务时，如果用户的 Prompt 非常长（如 32k），Prefill 阶段会耗时几秒导致首字延迟（TTFT）很高，容易触发客户端超时；而在 Decode 阶段，由于带宽瓶颈，即使 GPU 算力利用率只有 30%，吞吐量也无法提升。此时如果使用了 Speculative Decoding（投机采样），可以显著减少 Decode 的步数，从而缓解带宽压力。

- **代码示例**：
```python
# 伪代码：展示 Prefill 和 Decode 的 KV Cache 处理差异

kv_cache = {'k': [], 'v': []} 

def prefill_stage(model, prompt_tokens):
    # 输入是整个 prompt
    logits = model(prompt_tokens)
    # Prefill 结束后，通常一次性获取并缓存所有 K, V
    # kv_cache['k'] = all_keys_from_layers  
    return logits

def decode_stage(model, next_token):
    # Decode 输入只有一个 token
    # 关键：传入 kv_cache，模型只计算当前 token 的 Q, K, V
    # Q * (Cached_K) -> Attention Score
    logits = model(next_token, past_key_values=kv_cache)
    # 更新 cache: append(new_k, new_v)
    # kv_cache['k'].append(new_k)
    return logits
```

- **对比表格**：

| 维度 | Prefill 阶段 | Decode 阶段 |
| :--- | :--- | :--- |
| **输入形态** | 大 Batch 或长 Sequence | 单个 Token 或极小 Batch |
| **计算类型** | 矩阵-矩阵乘 | 矩阵-向量乘
| **瓶颈资源** | GPU Compute (FLOPS) | Memory Bandwidth (显存带宽) |
| **并行度** | 高度并行 | 严格串行（依赖上一个 Token）
| **GPU 利用率** | 高（接近 100%） | 低（通常 < 40%） |
| **优化目标** | 降低首字延迟 (TTFT) | 提高生成吞吐量 (TPS) |

## 常见考点
1.  **为什么 Decode 阶段 GPU 利用率低？**：因为计算量小而数据搬运量大，属于 Memory-bound 任务，而非 Compute-bound。
2.  **什么是 KV Cache PagedAttention (如 vLLM)？**：为了解决显存碎片化和动态 Batch 问题，将 KV Cache 分页存储，类似操作系统的虚拟内存管理。
3.  **如何优化 Decode 阶段的吞吐量？**：使用 Continuous Batching（连续批处理）或 Speculative Decoding（投机采样）。
