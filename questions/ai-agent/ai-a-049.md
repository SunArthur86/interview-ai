---
id: ai-a-049
difficulty: L3
category: ai-agent
subcategory: Agent评估
tags:
- 异常
- IOC
- Elasticsearch
feynman:
  essence: 在关键节点引入人类干预，确保方向正确且风险可控。
  analogy: 像新员工带老员工，拿不准的问一声，危险的操作递过来签字。
  first_principle: 如何让人类有效地参与AI的决策循环以弥补其不足？
  key_points:
  - 四种协作模式：Copilot辅助、Review审批、Supervise监督、Delegate委托
  - 在低置信度、高风险、计划阶段引入人工确认
  - 设计时需最小化对用户的打扰，但在必要时必须阻断
follow_up:
- 如何评估Agent的置信度?
- 用户拒绝后Agent如何调整计划?
---

# Human-in-the-Loop在Agent中如何设计?不同的人机协作模式有哪些

- **人机协作模式:**

| 模式 | 人 | Agent | 适用 |
|------|-----|--------|------|
| **Copilot** | 主导 | 辅助 | IDE/写作 |
| **Review** | 审批 | 执行 | 代码/文档 |
| **Supervise** | 监督 | 自主 | 自动化任务 |
| **Delegate** | 委托 | 全权 | 长任务 |

- **Human-in-Loop触发时机:**

1. **低置信度时** - Agent不确定时主动求助
2. **高风险操作** - 删除/部署/支付前
3. **计划审批** - 执行前展示计划
4. **定期检查** - 每N步展示进度
5. **异常处理** - 错误恢复时

- **设计模式:**

```python
# 模式1:计划审批
plan = agent.create_plan(task)
if user.approve(plan):
    agent.execute(plan)

# 模式2:逐步确认
for step in plan.steps:
    if step.risk_level >= 3:
        if not user.confirm(step):
            step.modify_based_on(user_feedback)
    agent.execute(step)

# 模式3:低置信度求助
if agent.confidence(result) < 0.7:
    result = user.ask_for_guidance()
```

- **关键原则:**
- 最小化打断(不要每步都问)
- 提供足够的上下文(让用户能做判断)
- 记住用户决策(类似的以后自动处理)

- **人机交互反馈循环:**
```text
                ┌───────────────┐
                │  User Intent  │
                └───────┬───────┘
                        ▼
                ┌───────────────┐
                │  Agent Plan   │───────┐
                │  Generation   │       │ (Plan Review)
                └───────┬───────┘       ▼
                        │        ┌───────────────┐
                        │        │ Human Feedback│
                        │        └───────┬───────┘
                        ▼                │
                ┌───────────────┐        │
                │  Agent Action │◄───────┘
                └───────┬───────┘
                        ▼
                ┌───────────────┐
                │   Result      │───────┐
                └───────────────┘       │ (Execution Check)
                                        ▼
                                ┌───────────────┐
                                │ Human Verify  │
                                └───────────────┘
```

- **进阶设计细节:**
   - **上下文压缩**: 向用户确认时，自动隐藏中间无关的步骤，只展示关键决策点和变更摘要。
   - **偏好学习**: 系统记录用户在确认过程中的修改习惯，后续自动应用类似修改（即 AutoGPT 的微调版）。
   - **多模态交互**: 对于图形界面任务，Agent 可以截图高亮需要操作的区域，询问用户是否点击，而非仅用文本描述。

- **实战案例**: 在自动化DevOps流程中，Agent遇到“合并冲突”时直接卡死。改进后，Agent将冲突文件diff通过Git Web界面链接发送给人工，并附上“建议Accept Incoming”的选项，将人工介入时间从30分钟缩短至2分钟。

- **代码示例**:
```python
from pydantic import BaseModel

class HumanInputRequest(BaseModel):
    question: str
    context: str
    options: list[str] = []  # 提供选项以降低输入门槛

def get_human_confirmation(agent_action: str):
    """中断Agent执行并请求人工输入"""
    req = HumanInputRequest(
        question=f"即将执行: {agent_action}. 是否继续?",
        context="当前步骤属于高风险操作类别",
        options=["Yes", "No", "Modify"]
    )
    # 此处挂起Agent，将req发送到前端界面等待WebSocket回调
    return wait_for_user_response(req)
```

- **协作模式深度对比**:
| 维度 | Copilot (副驾) | Delegate (委托) | Review (审查) |
|------|----------------|----------------|---------------|
| **决策主体** | 人类 | Agent (初始) -> 人类 | Agent (执行) / 人类 (把关) |
| **中断频率** | 极高 (按需触发) | 低 (仅在里程碑) | 中 (仅在Action前) |
| **信任要求** | 低 (随时接手) | 高 (需容错能力) | 中 (需可逆性) |
| **适用任务** | 创意、代码补全 | 数据清洗、长流程跑批 | 部署、发布、交易 |
