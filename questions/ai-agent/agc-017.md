---
id: agc-017
difficulty: L1
category: ai-agent
subcategory: Agent基础概念
feynman:
  essence: 单兵作战成本低，团队协作能力强但沟通开销大。
  analogy: 一个人干活效率高，一帮人干活能干大活但得开会协调。
  first_principle: 如何在单一实体与协作网络间做出最佳架构选择？
  key_points:
  - 单 Agent 结构简单，适合线性任务
  - 多 Agent 适合角色分工明确、可并行的复杂任务
  - 需权衡协调成本与任务收益
follow_up:
- 多 Agent 的通信成本和单 Agent 的上下文膨胀，哪个更难解决？
- 什么任务必须用多 Agent？
---

# 多 Agent 协作和单 Agent 多工具怎么选

**选型核心逻辑**：单 Agent 多工具适合任务边界清晰、逻辑线性的场景，优势在于实现简单、链路延迟低、上下文连贯性强；多 Agent 适合复杂任务，通过角色分工（如编码、测试、Review）、并行探索及对抗审查（如「批评者 Agent」）提升质量，但会带来协调成本、上下文拼接膨胀及一致性问题。选型需权衡任务分解难度、团队组织边界、延迟敏感度与 Token 成本。

**实战案例**：在自动化代码审查场景中，单 Agent 往往会“放过”自己生成的 Bug，而采用“生成 Agent + 审查 Agent”的多 Agent 模式，审查 Agent 会专门寻找安全漏洞，Bug 检出率提升约 40%。

**架构对比图：**

```text
┌─────────────────────────────────────────────────────────────┐
│                    任务复杂度 vs 控制力                      │
├───────────────────────────────┬─────────────────────────────┤
│      单 Agent (Orchestrator)   │      多 Agent (Multi-Agent) │
├───────────────────────────────┼─────────────────────────────┤
│  ┌──────────────┐             │  ┌──────────┐  ┌──────────┐  │
│  │   LLM Core   │             │  │ Manager  │  │ Critic   │  │
│  │  + Plugins   │             │  │  (Planner)│  │(Reviewer) │  │
│  └──────┬───────┘             │  └─────┬────┘  └─────┬────┘  │
│         │ Tool Calls          │        │             │       │
│    ┌────▼─────┐ ┌─────────┐   │  ┌─────▼─────┐  ┌────▼────┐  │
│    │  Search  │ │  Code   │   │  │Researcher│  │Coder   │  │
│    └──────────┘ └─────────┘   │  └──────────┘  └─────────┘  │
│  [Context 共享，低延迟]       │  [消息传递，高并⾏，高开销]   │
└───────────────────────────────┴─────────────────────────────┘
```

**代码示例（伪代码对比）：**
```python
# 单 Agent：顺序执行
def single_agent(task):
    plan = llm.generate(f"Make a plan for: {task}")
    for step in plan:
        result = tools[step.tool].call(step.args)
    return result

# 多 Agent：分发与协作
def multi_agent(task):
    manager = Agent("Manager")
    sub_tasks = manager.delegate(task) # 分解任务
    # 并行执行
    results = run_parallel([Agent("Worker").do(t) for t in sub_tasks])
    # 审查
    final = Agent("Critic").review(results)
    return final
```

**对比表格：架构模式对比**

| 维度 | 单 Agent 多工具 | 多 Agent 协作 |
| :--- | :--- | :--- |
| **上下文连贯性** | 高（思维链在同一 Context） | 中低（需跨 Agent 传递状态，易信息丢失） |
| **延迟与成本** | 低（单次 LLM 调用链路） | 高（多轮对话、多次推理、通信开销） |
| **容错性** | 差（一步错步步错） | 好（可通过 Critic Agent 纠错或重试） |
| **适用场景** | 查询、简单操作、线性任务 | 复杂软件开发、模拟辩论、多步推理 |

**补充细节**：
1. **单 Agent 的边界**：当工具调用之间存在严格依赖且无需并行时，单 Agent 的链式思考效果最好。
2. **多 Agent 的通信模式**：常见的有“广播式”（所有 Agent 共享黑板）和“接力式”（Manager 分发子任务），前者上下文消耗快，后者管控容易但灵活性稍低。
3. **一致性问题**：多 Agent 环境下，需设定“共享记忆”或“全局状态源”，避免不同 Agent 对事实认知产生分歧（例如上下文窗口限制导致遗忘）。

## 常见考点
1. **多 Agent 中的「死锁」或「无限循环」如何解决？**（答：设置最大步数、超时机制、裁决 Agent）
2. **如何评估多 Agent 比 单 Agent 带来的性能提升是否值得成本？**（答：使用 AB 测试对比任务完成率、Token 消耗与端到端延迟）
3. **单 Agent 处理长任务时的「中间迷失」问题怎么缓解？**（答：Checkpoint 机制、子任务摘要总结）
