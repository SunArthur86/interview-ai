---
id: agc-001
difficulty: L1
category: ai-agent
subcategory: Agent基础概念
images:
- svg_agent.svg
feynman:
  essence: 具备自主决策与任务执行能力的智能系统。
  analogy: 有脑子、会记事、能动手的智能助手，而不只是嘴炮。
  first_principle: 如何让机器像人一样自主感知环境并解决问题？
  key_points:
  - 以大模型为核心大脑
  - 具备闭环的感知-决策-行动能力
  - 能利用工具和记忆处理复杂任务
follow_up:
- Agent 和 Workflow（固定流程编排）的核心区别是什么？
- 一个只能调用单个工具的 LLM 应用算 Agent 吗？
---

# 一句话说明什么是 AI Agent

**AI Agent 是以大模型（LLM）为「大脑」，具备感知环境、规划推理、调用工具并利用记忆进行多步交互的智能系统。**

它不仅能生成文本，更能通过「感知-思考-行动」的闭环，在不确定的环境中自主决策以完成复杂目标。

### 核心要素补充：
- **自主性**：无需人工干预即可处理流程中的异常。
- **社会化能力**：部分高级 Agent 具备协作能力（如多 Agent 系统），通过沟通解决更复杂问题。

**核心闭环流程图：**
```text
      ┌───────────────────────────────────────┐
      │           User Goal (用户目标)          │
      └───────────────────┬───────────────────┘
                          │
                          ▼
      ┌───────────────────────────────────────┐
      │          Perception (感知)             │
      │  (输入: Prompt, Image, State, etc.)    │
      └───────────────────┬───────────────────┘
                          │
                          ▼
      ┌───────────────────────────────────────┐
      │       Brain / LLM (思考与决策)         │
      │  ┌─────────┐  ┌──────────┐  ┌─────┐   │
      │  │Planning │  │  Memory  │  │Tools│   │
      │  └────┬────┘  └─────┬────┘  └──┬──┘   │
      └───────┼──────────────┼─────────┼───────┘
              │              │         │
              ▼              │         ▼
      ┌───────────────┐      │   ┌───────────┐
      │  Action (行动)│      │   │  External │
      │ (调用API/文本)│      │   │ Knowledge/│
      └───────┬───────┘      │   │  Systems  │
              │             │   └─────┬─────┘
              │             │         │
              ▼             │         │
      ┌───────────────────────────────────────┐
      │     Environment / Observation (反馈)  │
      └───────────────────┬───────────────────┘
                          │
              ┌───────────┴───────────┐
              ▼                       ▼
        (完成/失败)               (继续下一轮思考)
```

**实战案例：**
在部署 GitHub Issue 自动修复 Agent 时，我们发现简单的“理解-修复”闭环常因环境配置（如缺少依赖）导致失败。通过引入“感知”层（运行 `docker ps` 和日志报错读取），Agent 能自动检测环境差异并动态调整构建命令，将修复成功率从 30% 提升至 75%。

**代码示例（Python/LangChain）：**
```python
from langchain.agents import initialize_agent, Tool, AgentType
from langchain.utilities import SerpAPIWrapper

# 定义工具（感知与行动的延伸）
search = SerpAPIWrapper()
tools = [
    Tool(name="Search", func=search.run, description="用于获取实时信息")
]

# 初始化 Agent，实现“感知-思考-行动”闭环
agent = initialize_agent(
    tools, llm, agent=AgentType.ZERO_SHOT_REACT_DESCRIPTION, verbose=True
)
```

## 面试追问
1. **状态管理与无状态性**：LLM 本身是无状态的，Agent 如何在多轮交互中维持任务状态？
2. **容错机制**：如果 Agent 调用工具失败（如 API 超时），在架构设计中通常有哪些兜底策略？
3. **评估指标**：除了成功率，如何量化评估 Agent 的“规划能力”或“推理质量”？

## 易错点
1. **过度拟人化**：认为 Agent 具备类似人类的“意识”。实际上 Agent 的“智能”完全来自 LLM 的概率预测和模式匹配，在处理未见过的逻辑陷阱时仍可能表现出愚蠢的行为。
2. **混淆工具与模型**：误认为接入了越多工具 Agent 就越强。实际上，工具过载会导致 LLM 在决策时面临“注意力分散”和“幻觉加剧”，反而降低准确率。
