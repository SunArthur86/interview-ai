---
id: agm-020
difficulty: L3
category: ai-agent
subcategory: 记忆系统
feynman:
  essence: 预留安全余量并按优先级动态分配 Token 资源。
  analogy: 像装修预算，大头留硬装（任务），软装（历史）适可而止，必须留备用金。
  first_principle: 如何在有限上下文窗口内实现资源的最优配置？
  key_points:
  - 余量：强制保留20-30%缓冲
  - 配比：任务优先，历史压缩，文档精排
  - 压缩：旧对话摘要，新对话保留
  - 截断：大文本取TopK或摘要
follow_up:
- 如何自动检测上下文即将溢出?
- 摘要压缩的最佳时机?
---

# Agent记忆的Token预算如何分配才不容易翻车?不同记忆类型的Token配比建议

### Token 预算分配框架（假设总上下文 128K tokens）
- **System Prompt**: 2K (1.5%)
- **工具定义**: 3K (2.5%)
- **长期记忆检索**: 10K (8%)
- **对话历史(近期)**: 15K (12%)
- **RAG 文档检索**: 30K (24%)
- **当前任务输入**: 5K (4%)
- **输出预留**: 4K (3%)
- **剩余空间（安全余量）**: 59K (46%)

### 关键原则
1.  **永远留余量**：不要把上下文填满，留 20-30% 余量。
2.  **动态调整**：根据任务类型调整配比。
3.  **优先级排序**：当前任务 > 对话历史 > RAG 文档 > 长期记忆。

### 记忆 Token 控制策略
| 记忆类型 | 压缩方式 | 目标 Token |
|---------|---------|-----------|
| 对话历史 | 滑动窗口(旧) + 摘要(更旧) + 原文(最新3轮) | <15K |
| 工具输出 | 截断(取前/后部) 或 结构化提取 | <5K/次 |
| RAG 文档 | 精排(Cross-Encoder取 Top-3) | <30K |
| 长期记忆 | 检索(取相关 Top-5) + 重要性过滤 | <10K |

### 翻车场景与应对
- **场景1**：工具返回 10MB 日志 → **撑爆上下文**
  - **对策**：LLM 无法直接阅读。必须在 Tool 层做预处理（提取 Error 级别日志、统计摘要），只传结果给 LLM。
- **场景2**：对话 100 轮未压缩 → **超限**
  - **对策**：当历史超过阈值（如 8K），触发异步摘要任务，将旧对话替换为 Summary。
- **场景3**：RAG 返回 50 个文档 → **太多噪音**
  - **对策**：必须使用两阶段检索（向量粗排 + 重排 Rerank），严格控制进入 Context 的文档数量。

### 深化实战
- **实战案例**：曾遇客户上传 200 页 PDF 进行问答，直接截断导致结论错误。后改为在 Prompt 中增加“如果你的回答中没有引用到具体文档内容，请明确告知用户信息不足”，有效降低幻觉。
- **代码示例**：
```python
def manage_context(history, limit=12000):
    # 动态压缩策略：保留最新3轮，其余按Token截断
    recent = history[-3:]
    old = history[:-3]
    # 递归压缩或截断旧消息
    if count_tokens(old) > limit / 2:
        old = truncate_text(old, limit/2) # 简单截断或调用摘要模型
    return old + recent
```

### 边界情况补充
1.  **输出突发**：如果模型生成长度预估失败，导致输出超出 `输出预留` 空间，需要在代码层实现 **Early Stopping** 或 **Fuzzy Truncation**（在句子末尾截断而非强行截断 Token），以防止生成不完整的 JSON 或代码块导致下游解析器崩溃。
2.  **多模态输入**：当输入包含图片或音频时，这些内容通常通过 Tokenizer 转换为大量 Token（如一张高清图可能占 2K+ Tokens）。在预算分配时必须预先扣除这部分“硬成本”，若图片过大需在预处理阶段进行视觉压缩。
3.  **Prompt 膨胀**：随着功能迭代，System Prompt 可能会不知不觉膨胀（如加入大量few-shot示例）。应建立 **Prompt 版本管理** 和 Token 监控机制，防止 System Prompt 挤占 RAG 或对话历史的空间。

### ## 面试追问
1. 如果用户的任务输入本身就超过了总 Context 的一半（例如上传了超长文本），你是如何处理“截断”策略的？简单截断中间部分会不会丢失核心语义？有没有更好的“滑动窗口截断”或“语义分段截断”方案？
2. 在多轮对话中，如果摘要模型生成的 Summary 丢失了关键细节（如用户的姓名设定），导致后续回答出现事实性错误，有什么机制可以检测或回滚这种“信息熵减”带来的损失？
3. 对于 RAG 检索，如果高分的 Top-K 文档高度相似（同一段话被切分成了多个 chunk），占用 Token 但信息量低，你会如何做去重或多样性处理？

### ## 易错点
1. **忽视编码开销**：很多计算器只计算文本的 Token 数，忽略了 Function Call 中的参数（如 JSON 结构体）以及特殊 Token（如 `<|im_start|>`）也会计入 Context 窗口，导致实际请求时超出限制。
2. **摘要的“信息坍塌”**：认为无限做摘要就能无限压缩。实际上，随着层级加深，摘要会丢失大量细节。对于关键事实（如银行卡号、ID），应采用“长期记忆向量库”存储，而不是压缩进摘要里，否则会永久丢失。
