---
id: agp-001
difficulty: L1
category: ai-agent
subcategory: Prompt工程
feynman:
  essence: 用自然语言控制程序逻辑
  analogy: Prompt 就像指挥机器人的“口头指令清单”，改清单等于改程序。
  first_principle: 如何用自然语言灵活定义计算逻辑
  key_points:
  - 行为逻辑编码在 Prompt 中
  - 变更影响大，需版本管理
  - 属于动态配置而非静态代码
follow_up:
- Prompt 作为「软代码」，如何做版本管理和回归测试？
- Prompt 的「可维护性」和「效果」如何平衡？
---

# 为什么说 Prompt 是 Agent 的「软代码」

因为 Agent 的行为策略（何时触发推理、何时调用工具、输出什么格式）大量编码在 System/User Prompt 与模板里。修改 Prompt 就像修改业务逻辑代码，直接影响系统输出，因此必须进行版本管理、Code Review（同行评审）以及 A/B 测试，以确保行为的稳定性和可复现性。

**软代码与硬代码协作流程：**
```text
┌──────────────────────────────────────┐
│         Agent 运行时系统              │
├──────────────────────────────────────┤
│                                      │
│  ┌──────────────┐     ┌─────────────┐│
│  │  Prompt 模板  │────▶│   LLM 模型   ││
│  │  (软代码)     │     │  (解释器)    ││
│  └──────┬───────┘     └──────┬──────┘│
│         │                     │      │
│         │ 生成指令             │      │
│         ▼                     │      │
│  ┌─────────────────────────────┐│     │
│  │      Python/JS 程序         │◄─────┘
│  │      (硬代码: 解析JSON/     │
│  │       调用API/ 异常处理)   │
│  └─────────────────────────────┘│
└──────────────────────────────────────┘
```

## 常见考点
1. **Prompt 注入防护**：既然 Prompt 是代码，如何防止用户输入恶意 Prompt 篡改系统指令？（使用分隔符、护栏模型或指令转义）。
2. **版本控制**：如何管理 Prompt 的迭代？（类似 Git 管理，配合 Prompt 管理平台如 LangSmith）。
3. **调试难度**：Prompt 是非确定性的，如何调试？（使用 Log 记录中间思考过程、温度参数设为 0）。
