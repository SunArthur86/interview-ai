---
id: agl-002
difficulty: L1
category: ai-agent
subcategory: 大模型基础
tags:
- IO
images:
- svg_transformer_blocks.svg
feynman:
  essence: Encoder 看全局，Decoder 只看历史且需融合源信息。
  analogy: 读书看全文 vs 写作只能看前面写的字并参考素材。
  first_principle: 如何区分信息编码与生成过程中的可见性？
  key_points:
  - Encoder：双向注意力
  - Decoder：Causal Mask（自回归）
  - Seq2Seq：Decoder 包含 Cross-Attention
follow_up:
- Decoder 的 causal mask 如何实现？
- Encoder 双向注意力对什么任务更合适？
---

# Encoder 和 Decoder 的自注意力有何不同

**核心区别**：
1. **Encoder Self-Attention**：**双向（Bidirectional）**。所有 Token 可以互相看见，没有掩码限制。适用于理解上下文全局信息（如 BERT、文本分类）。
2. **Decoder Self-Attention**：**因果（Causal / Autoregressive）**。引入 Upper Triangular Mask（上三角掩码），当前位置只能看见自己和之前的位置。适用于生成任务（如 GPT）。
3. **Cross-Attention**：存在于 Seq2Seq 结构的 Decoder 中。Query 来自 Decoder（当前时刻），Key 和 Value 来自 Encoder（源序列输出）。这使得生成过程能够关注源序列的不同部分。

**架构对比图**：
```text
Encoder Self-Attn:      Decoder Self-Attn (Masked):
  1 2 3 4                 1 2 3 4
1 X O O O               1 X - - -  (- 表示Mask)
2 O X O O               2 O X - -
3 O O X O               3 O O X -
4 O O O X               4 O O O X

Cross-Attn (in Decoder):
  (Decoder Q)  x  (Encoder K, V)
```

## 常见考点
1. **Prefix-LM**：有些模型（如 GLM）如何结合两者优势？
2. **KV Cache**：在推理时，Causal Mask 对 KV Cache 的构建有何影响？
3. **掩码实现**：如何在代码中高效实现 Causal Mask（如 Additive Mask vs Multiplicative Mask）？
