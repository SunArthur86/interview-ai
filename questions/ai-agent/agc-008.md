---
id: agc-008
difficulty: L1
category: ai-agent
subcategory: Agent基础概念
feynman:
  essence: 目标型看成败，效用型算性价比。
  analogy: 导航：目标型只管到没到，效用型还要算路程短、不堵车。
  first_principle: 在多重约束下如何做出最优决策？
  key_points:
  - 目标型是二元判断（达成或未达）
  - 效用型在多冲突目标中权衡
  - 效用型考虑成本、风险等综合因素
  - 效用型追求全局最优解
follow_up:
- 基于目标的 Agent 在开放式任务上更鲁棒吗？
- 效用函数怎么设计才能避免 reward hacking？
---

# 基于效用的 Agent 和基于目标的有什么区别

目标型关注「是否达成」；效用型在多个冲突目标间做权衡（成本、时延、风险、用户偏好），选综合最优而非单点达成。

### 深度解析与数学原理

**1. 目标型 Agent**
- **核心逻辑**：基于二元状态。设定一个明确的 Goal $G$，状态空间 $S$ 中存在目标状态 $S_{goal}$。
- **判据**：$Success = (CurrentState == S_{goal})$。
- **适用场景**：逻辑解谜、迷宫寻路、代码生成（通过测试用例即成功）。
- **局限**：无法处理「有多条路径，但路径优劣不同」的情况，也无法处理「无法完全达成，但需要部分妥协」的情况。

**2. 效用型 Agent**
- **核心逻辑**：基于最大化效用函数 $U(s, a)$。Agent 不追求绝对达成，而是追求行动序列带来的期望收益最大化。
- **权衡维度**：
  - $Cost$：Token 消耗、API 调用费用、计算时间。
  - $Risk$：操作失败概率、数据安全风险。
  - $Preference$：用户个性化偏好（如「结果越快越好，哪怕准确率略降」）。
- **决策公式**：$Action^* = \operatorname{argmax}_{a} \sum_{s'} P(s'|s,a) \cdot U(s')$。

**3. 决策对比示意图**
```text
场景：预定去机场的出租车，有两个方案

方案 A：豪华专车，10分钟到，费用 $100
方案 B：经济拼车，30分钟到，费用 $20

[ Goal-Based Agent ]
User Goal: "预定一辆车"
Result: A 或 B 均可，可能随机选一个（或者选第一个满足条件的）。
Failure Mode: 可能选了 $100 的车，导致用户不满，因为 Goal 只定义了「预定」。

[ Utility-Based Agent ]
Utility Function: U = 0.6 * (1/CostNorm) + 0.4 * (1/TimeNorm)
Result: 根据 Utility 分数计算，方案 B 可能得分更高（若用户对价格敏感），
         Agent 选择 B。
Success Mode: 自动在冲突目标中找到帕累托最优解。
```

**4. 实际应用**
- 在企业级 Agent 中，Utility Function 往往通过 `Weights` 配置。例如：对于金融类任务，将「准确性」权重设为 0.9，「速度」设为 0.1；对于闲聊机器人，则反之。

### 边界情况补充
- **效用函数饱和**：当某个指标（如速度）达到极致后，继续优化带来的边际效用极低，Agent 可能陷入无效优化。需引入对数或 Sigmoid 函数作为效用映射，避免极端值主导。
- **权重漂移**：用户的偏好可能随时间动态变化（如白天追求速度，晚上追求成本）。效用函数的权重应是动态可调的，而非静态硬编码。
- **不确定性下的决策**：在无法获知确切概率 $P(s'|s,a)$ 的场景下（如全新工具），需要引入探索与利用机制，防止陷入局部效用最高的次优解。

### 实战补充

**实战案例**：在构建数据检索 Agent 时，Goal-Based 只要找到文档就认为成功，但往往召回几千条无关文档导致用户阅读负担重。改为 Utility-Based 后，引入「相关性」和「文档长度」作为负向效用，Agent 会自动选择先用精简的 Summary API 概述，而不是直接 Dump 全文，极大提升了体验。

**代码示例**：
```python
def calculate_utility(action, context):
    score = 0.0
    # 效用函数定义：权重 + 归一化因子
    if action.tool == "search_expensive_db":
        score -= 0.8  # 成本惩罚
    if action.latency > 5.0:
        score -= 0.5 * (action.latency / 5.0) # 延迟惩罚
    if "high_confidence" in action.tags:
        score += 1.0 # 准确性奖励
    return score

# Agent 决策循环时
best_action = max(candidates, key=lambda a: calculate_utility(a, ctx))
```

**类型对比**

| 维度 | 目标型 Agent | 效用型 Agent |
| :--- | :--- | :--- |
| **决策依据** | 是否满足预设条件 | 是否最大化期望收益 |
| **输出结果** | 布尔值（成功/失败） | 实数值（评分/排序） |
| **处理冲突** | 无法处理，通常取首个可行解 | 自动权衡多维指标（如速度vs成本） |
| **复杂度** | 低，逻辑清晰 | 高，需设计并调优效用函数 |
| **典型场景** | 游戏通关、格式校验 | 资源调度、推荐系统、复杂交易 |

## 面试追问
1. 如何为一个多目标优化的 Agent 确定各个维度的权重？如果业务方对权重有争议，怎么解决？
2. 在动态环境中，如果两个候选 Action 的效用函数得分极其接近（如只差 0.001），引入什么机制来打破僵局，避免系统震荡？

## 易错点
1. **效用函数的唯分数论**：认为 Utility 分数高就一定代表用户体验好。如果效用函数缺少某些关键维度（如安全性），Agent 可能为了高分而执行高风险操作（如越权访问）。
2. **混淆目标与约束**：将必须满足的硬性约束（如「不得输出密码」）当作负向效用处理。约束应是过滤条件，效用是在满足约束后的排序依据。
