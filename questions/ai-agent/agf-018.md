---
id: agf-018
difficulty: L1
category: ai-agent
subcategory: Agent核心框架
feynman:
  essence: ReAct 适合动态交互，Plan 适合结构化分解任务。
  analogy: 即兴发挥的爵士乐 vs 严格按谱演奏的交响乐。
  first_principle: 如何平衡规划的预计算成本与执行的灵活性？
  key_points:
  - ReAct：强交互、动态环境
  - Plan-and-Execute：强分解、可审计计划
  - 权衡前期规划成本与灵活性
---

# 你如何为一个企业场景选择 ReAct vs Plan-and-Execute

**选择核心逻辑**：依据任务的**确定性**、**流程复杂度**与**交互成本**进行权衡。

**详细增强**：
1. **ReAct (Reason + Act)**：适合**动态环境**或**未知路径**的任务。特点是“边走边看”，交互频繁。场景：如客服问答、调试代码、实时信息查询。优势是灵活，劣势是容易迷失且过程不可审计。
2. **Plan-and-Execute (Plan then Solve)**：适合**目标明确**、**步骤可分解**的任务。特点是“先谋后动”，先生成完整计划表，再按步骤执行。场景：如多模块数据分析报告生成、复杂旅行规划。优势是全局视野强、过程可审计，劣势是前期规划 Token 消耗大，且一旦计划错误需全盘重推。
3. **边界情况**：对于长上下文且步骤极其繁琐的任务（如生成包含 50 个步骤的自动化脚本），Plan-and-Execute 可能会因为 Context Window 限制而无法一次性生成完整计划，此时需要结合“层次化规划”；而 ReAct 虽然理论上可以无限长，但在长链中极易产生“遗忘”现象，导致后期步骤与前期目标脱节。

**决策流程图**：
```text
        Start
          |
   环境是否高度动态? (需实时反馈)
   /           \
 Yes           No
  |             |
 ReAct      步骤是否可预判?
             /    \
          Yes     No
           |       |
 Plan-Exe  ReAct
 (需可审计性?) (兜底)
```

**实战案例**：
在开发“电商数据分析 Agent”时，初期使用 ReAct，结果模型在生成图表、计算指标时频繁迷失，经常计算完销售额又忘记录入时间范围，导致反复重试。改为 Plan-and-Execute 后，第一步先让模型列出完整计划（1.筛选数据 -> 2.计算GMV -> 3.生成趋势图 -> 4.输出结论），再逐条执行。这不仅让过程可被人类审计，还复用了同一个“筛选数据”的变量，节省了重复查询的成本。

**代码示例 (Python)**：
```python
# Plan-and-Execute 模式伪代码
def plan_and_execute_agent(goal):
    # Stage 1: Planner
    plan = llm.generate(f"Create a step-by-step plan for: {goal}")
    steps = parse_steps(plan)
    
    # Stage 2: Executor
    context = {}
    for step in steps:
        result = execute_step(step, context)
        context[step.id] = result # 传递上下文
        if result.status == 'fail':
            break # 计划失败需人工介入或全盘重推
    return context
```

**对比表格**：
| 维度 | ReAct | Plan-and-Execute |
| :--- | :--- | :--- |
| **路径规划** | 动态生成，每一步重新思考 | 静态生成，预先列出所有步骤 |
| **容错性** | 单步失败易修正，易迷失方向 | 单步失败往往导致整体计划崩溃 |
| **Token 成本** | 不可控 (取决于推理循环次数) | 前期高 (生成计划)，后期线性增长 |
| **适用性** | 探索性、开放式任务 | 逻辑严密、流程标准化任务 |

## 常见考点
1. **混合模式**：是否存在 ReAct 和 Plan-and-Execute 混合使用的场景？
2. **执行中断**：Plan-and-Execute 在执行过程中遇到不可抗力如何处理？
3. **成本分析**：在长上下文任务下，哪种模式的 Token 消耗更低？

## 面试追问
1. 上述提到 Plan-and-Execute 一旦计划错误需全盘重推，你会如何设计架构来支持“局部重规划”而不是“从头再来”？（提示：将计划拆分为子目标，失败时仅回滚当前子目标）
2. 在多轮对话中，用户经常中途修改需求（如“其实我不仅要查销售额，还要查利润”），这对哪种模式破坏性更大？为什么？（提示：Plan-and-Execute 需要重新生成和校验整个计划的一致性）
3. 如果任务的每一步执行时间极长（例如排队等待 GPU 资源），ReAct 的串行执行会导致总耗时过长，Plan-and-Execute 是否能解决这个问题？如何优化？（提示：识别计划中的独立步骤，并行执行）

## 易错点
1. **认为 Plan-and-Execute 一定更优**：在探索性任务中，强行规划会限制 Agent 的发挥，且规划阶段的幻觉一旦进入执行层会被放大。
2. **忽略规划的动态性**：Plan-and-Execute 并不是计划生成后就完全不可变，成熟的实现应该包含“执行中监控并根据反馈修正计划”的机制。
