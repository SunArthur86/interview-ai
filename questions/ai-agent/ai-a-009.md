---
id: ai-a-009
difficulty: L3
category: ai-agent
subcategory: Agent架构
tags:
- IO
- GC
- IOC
- Elasticsearch
feynman:
  essence: 基于状态机图结构构建支持循环和记忆的复杂Agent工作流。
  analogy: 像画流程图编程，节点是办事员，连线是传递规则，中间的公文包就是State。
  first_principle: 如何用图结构形式化描述和管理包含循环、分支的复杂Agent状态流？
  key_points:
  - 核心概念包括State（状态）、Node（节点）和Edge（边）
  - 原生支持循环，弥补了LangChain Chain只能线性的缺陷
  - 通过Checkpointer支持状态持久化和时间旅行
  - Conditional Edge实现基于状态的动态路由
  - 内置Interrupt机制支持人机协同与审核
follow_up:
- LangGraph的Checkpoint如何工作?
- Conditional Edge和Router有什么区别?
---

# LangGraph的核心概念是什么?它如何用图结构管理Agent状态

- **LangGraph:** 用有向图(StateGraph)编排Agent工作流.

- **核心概念:**

1. **State** - 全局状态对象(TypedDict/Pydantic)
2. **Node** - 图节点,每个节点是一个函数/Agent
3. **Edge** - 边,定义节点间转移
4. **Conditional Edge** - 根据状态动态选择下一节点

- **示例架构:**
```
START → research → draft → review
↑       ↓
revise ← critique
```

- **代码示例:**
```python
graph = StateGraph(AgentState)
graph.add_node("research", research_node)
graph.add_node("draft", draft_node)
graph.add_node("review", review_node)
graph.add_edge("research", "draft")
graph.add_conditional_edges("review", should_revise,
{True: "draft", False: END})
graph.add_edge(START, "research")
```

- **优势:**
1. **循环支持** - LangChain Chain不支持循环,LangGraph原生支持
2. **状态管理** - 全局状态自动传递
3. **可持久化** - Checkpoint支持中断/恢复
4. **人机协作** - Interrupt机制支持人工审批

- **状态流转详细架构图:**
```
+-------------------------------------+
|            LangGraph Runtime        |
|                                     |
|  ┌─────────────────────────────┐   |
|  │    Checkpointer (Memory)     │   |
|  └──────────────┬──────────────┘   |
|                 │ saves/loads      |
|                 ↓                  |
|  ┌─────────────────────────────┐   |
|  │      State (TypedDict)       │◄───┼─── Global Shared State
|  │  { messages, next_action }  │   |
|  └──────────────┬──────────────┘   |
|                 │ passes           |
|      ┌──────┬───┴───┬──────┐      |
|      ▼      ▼       ▼      ▼      |
|   [Node A][Node B][Node C][End]   |
|      │      │       │             |
|      └──►(Edges)◄───┘             |
|            │                      |
|    Conditional Edge Function       |
|    (decides next node based       |
|      on current state)            |
+-------------------------------------+
```

- **实战案例：**
在开发金融研报生成器时，使用了LangGraph的`interrupt`功能。当Agent需要调用高权限的交易数据库时，触发暂停，等待人工输入OTP验证码后才继续执行，有效避免了自动化的安全风险。

- **对比表格：LangChain Chain vs LangGraph**
| 特性 | LangChain Chain (Legacy) | LangGraph |
| :--- | :--- | :--- |
| **拓扑结构** | 线性/简单分支 | 有向循环图 (Cyclic Graph) |
| **状态管理** | 隐式传递 | 显式 StateGraph (TypedDict) |
| **流式控制** | 难以实现循环/回退 | 原生支持循环、条件跳转 |
| **人机交互** | 困难 | 原生支持 `interrupt` / `resume` |
| **持久化** | 手动实现 | 内置 Checkpointer (Memory/Snapshot) |

- **## 常见考点**
1. **状态冲突处理**：当多个并行节点修改同一个 State 字段时，LangGraph 的 Reducer 函数如何工作（如 `append_messages` reducer）？
2. **Checkpointer 机制**：如何利用 Checkpointer 实现“时间旅行”（Time Travel），即回滚到历史状态并改变分支执行？
3. **人机交互**：`interrupt` 机制在代码层面是如何暂停图执行并等待外部输入批准的？
