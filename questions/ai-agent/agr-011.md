---
id: agr-011
difficulty: L3
category: ai-agent
subcategory: 评估
tags:
- Elasticsearch
feynman:
  essence: 中文连续文本需切词统计
  analogy: 中文像连在一起的糖葫芦，要切开数一数有多少种果子。
  first_principle: 如何处理连续的中文文本以进行关键词匹配
  key_points:
  - BM25依赖分词
  - 需配置自定义词典
  - 混合检索弥补分词不足
follow_up:
- 向量检索为什么不需要分词?
- jieba如何添加自定义词典?
---

# BM25在中文场景下需要分词吗?中文全文检索有什么特殊处理

- **BM25在中文中的处理:**

- **需要分词!** BM25基于词项频率(TF)和逆文档频率(IDF),需要先将文本切分成词.

- **中文分词方案:**

| 工具 | 特点 | 
|------|------| 
| **jieba** | 最流行,支持自定义词典 | 
| **HanLP** | 功能全面,支持命名实体识别 | 
| **pkuseg** | 北京大学,学术领域效果好 | 
| **IK Analyzer** | Elasticsearch中文分词插件 |

- **Elasticsearch中文BM25:**
```json 
{ 
  "analysis": { 
    "analyzer": { 
      "chinese": { 
        "tokenizer": "ik_max_word" 
      } 
    } 
  } 
} 
```

- **ASCII 流程图 (中文BM25处理链路):**
```
原始文档: "自然语言处理技术发展迅速"
  │
  ▼ [中文分词]
["自然", "语言", "处理", "技术", "发展", "迅速"]
  │
  ▼ [去停用词]
["自然", "语言", "处理", "技术", "发展", "迅速"] (中文停用词较少)
  │
  ▼ [计算 TF-IDF]
Term: "技术" -> Doc_Freq, IDF_Score
  │
  ▼ [构建倒排索引]
Posting List: { "技术": [Doc1, Doc5, ...] }
  │
  ▼ [BM25评分]
Score(Q, D) = Σ IDF(qi) * (f(qi, D) * (k1 + 1)) / (f(qi, D) + k1 * (1 - b + b * |D|/avgdl))
```

- **中文BM25的坑:**
1. **分词错误** - 影响检索准确性 (如 "南京市长江大桥" 分成 "南京市长" / "江大桥")
2. **未登录词** - 专业术语/新词需要自定义词典
3. **混合检索** - 中文+英文+数字需要特殊处理 (混合分词器配置)
4. **同义词** - 中文同义词丰富,需要扩展 (如 "计算机" 和 "电脑")

- **替代方案:** BM25 + 向量检索混合,向量检索不需要分词。

## 常见考点
1. **ik_max_word 和 ik_smart 的区别**？（max_word最细粒度切分，适合索引；smart粗粒度切分，适合查询）
2. **BM25 参数 k1 和 b 在中文场景下如何调优**？（k1控制词频饱和度，b控制文档长度归一化，中文文档通常较短，b 可适当调低）
3. **为什么不直接用按字检索**？（由于中文单字语义信息量少于词项，按字检索会导致召回过高但准确率极低，噪音太多）
