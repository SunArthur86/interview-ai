---
id: agc-003
difficulty: L1
category: ai-agent
subcategory: Agent基础概念
tags:
- IO
feynman:
  essence: 固定流程脚本与动态自主决策的区别。
  analogy: Chain 是按剧本演电影，Agent 是根据现场情况即兴发挥。
  first_principle: 如何在确定性与不确定性之间选择任务执行策略？
  key_points:
  - Chain 路径固定，流程确定
  - Agent 路径动态，实时决策
  - 二者结合可兼顾稳定性与灵活性
follow_up:
- Prompt Chain 能否演化出「根据中间结果动态改路」的能力？
- 什么场景下 Prompt Chain 反而比 Agent 更合适？
---

# Agent 和 Prompt Chain 有什么本质区别

Agent 和 Prompt Chain 的本质区别在于 **「控制流的确定性」** 和 **「交互闭环」**。

1. **Prompt Chain (链式调用)**
   *   **本质**：静态的工程编排。开发者预先定义好了 A -> B -> C 的调用顺序和数据流转。
   *   **适用场景**：流程固定、逻辑清晰的任务（如：翻译 -> 摘要 -> 风格润色）。
   *   **局限性**：一旦某一步输出不符合预期，链很难自动回溯或调整路径。

2. **Agent (智能体)**
   *   **本质**：动态的自主决策。LLM 在每一步根据「观察」决定下一步动作，路径是运行时生成的树状或网状结构。
   *   **适用场景**：目标明确但路径模糊、需要处理未知错误的任务（如：查资料 -> 发现缺失关键词 -> 重新搜索 -> 整合）。
   *   **优势**：具备试错、调整和自我纠错的能力。

**拓扑结构对比图：**
```text
  Prompt Chain (Linear/Pipeline):      Agent (Dynamic Graph):

  ┌───┐    ┌───┐    ┌───┐             ┌───────┐
  │ A │───▶│ B │───▶│ C │───▶ Result  │ Start │
  └───┘    └───┘    └───┘             └───┬───┘
   (Fixed Path)                        │
                                       ▼
                                 ┌─────────┐   ┌─────────┐
                                 │ Choice 1│   │ Choice 2│ (根据环境反馈选择)
                                 └────┬────┘   └────┬────┘
                                      │             │
                                      ▼             ▼
                                 ┌─────────┐   ┌─────────┐
                                 │Observ-  │   │Observ-  │
                                 │ation    │   │ation    │
                                 └────┬────┘   └────┬────┘
                                      │             │
                                      └──────┬──────┘
                                             ▼
                                         (Result)
```

**实战案例：**
在做客服工单分类时，我们先用 Chain 提取关键词和意图，准确率极高且稳定（99%）。但在处理“客户退货退款”这类需要查询订单状态、计算时间、判断策略的复杂任务时，Chain 会因为中间某一步 API 失败直接报错中断。改用 Agent 后，它能自动识别 API 超时并重试，甚至在缺少订单号时自主追问用户，任务完成率提升了 40%。

**代码示例（LangChain 概念对比）：**
```python
# Prompt Chain: 硬编码顺序，确定性高
chain = (
    prompt_1 | model_1 | parser_1 |  # Step A: 提取信息
    prompt_2 | model_2 | parser_2    # Step B: 基于A的结果翻译
)
chain.invoke({"text": "Hello"})

# Agent: 运行时决策，动态路由
agent = Agent(
    tools=[search_tool, db_tool], 
    model=llm,
    type="structured-chat-zero-shot"
)
# LLM 自主决定是先 Search 还是先查 DB，或者两者都做
agent.invoke({"input": "分析一下竞争对手价格"})
```

**对比表格：**

| 维度 | Prompt Chain (链式) | Agent (智能体) |
| :--- | :--- | :--- |
| **控制权** | 开发者 | LLM (运行时决策) |
| **路径确定性** | 静态固定 (A->B->C) | 动态生成 (树状/网状) |
| **容错能力** | 弱 (一步错全盘错) | 强 (具备自我纠错机制) |
| **延迟与成本** | 低 (单轮或少轮) | 高 (多轮推理与尝试) |
| **适用场景** | 标准化流水线、ETL、翻译 | 复杂问题求解、故障排查、RPA |

## 常见考点
1. **混合模式**：在实际工程中，常用 Chain 处理稳定逻辑，用 Agent 处理不确定性较高的子环节（LangChain 的 LangGraph 思想）。
2. **性能对比**：Chain 延迟低且可预测；Agent 延迟高（多轮交互）但更灵活。
3. **调试难度**：Chain 容易调试；Agent 的行为具有概率性，如何 Trace 和评估 Agent 的路径是难点。
