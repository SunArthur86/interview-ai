---
id: agc-002
difficulty: L1
category: ai-agent
subcategory: Agent基础概念
images:
- svg_agent.svg
feynman:
  essence: 通过规划、记忆、工具赋予大模型行动能力。
  analogy: 只有大脑（LLM）不够，还得有手脚、计划和记性才能干活。
  first_principle: 如何补全 LLM 缺失的思考深度、持久记忆与物理交互？
  key_points:
  - 规划拆解长任务路径
  - 记忆维持上下文连续性
  - 工具赋予现实交互能力
follow_up:
- 如果去掉 Memory，Agent 还能完成多步任务吗？
- Planning 和 Tools 哪个更难做好？为什么？
---

# 为什么说 Agent = LLM + Planning + Memory + Tools?缺一块会怎样

LLM 仅仅是「大脑」或推理引擎，而完整的 Agent 需要四肢、记忆和策略。这四者构成有机整体，缺一不可：

**1. LLM (Core / 大脑)**
*   **作用**：负责理解意图、逻辑推理、指令生成。
*   **缺失后果**：系统失去通用认知能力，退化为传统的基于规则的脚本。

**2. Planning (规划 / 策略)**
*   **作用**：将大目标拆解为子任务，如 CoT（思维链）、ReAct 模式。处理任务依赖关系和执行顺序。
*   **缺失后果**：面对复杂任务无法拆解，容易陷入「一步错步步错」，只会做单次指令响应，无法处理长程任务。

**3. Memory (记忆 / 上下文)**
*   **作用**：短期记忆（缓存上下文）、长期记忆（向量数据库 RAG）。记录历史交互、关键事实和中间结果。
*   **缺失后果**：无法从历史中学习，多轮对话中会「失忆」，无法处理需要跨步骤累积信息的任务。

**4. Tools (工具 / 执行)**
*   **作用**：扩展能力边界，如联网搜索、代码解释器、ERP 系统接口。
*   **缺失后果**：LLM 被困在训练数据的时间截止点前，无法获取实时信息，无法对物理世界产生实际影响（只能「空谈」）。

**系统架构图：**
```text
                ┌─────────────────────────────────────┐
                │           User Input                │
                └─────────────┬───────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────┐
    │                   AGENT FRAMEWORK                    │
    │  ┌───────────────────────────────────────────────┐  │
    │  │             PLANNING (Controller)              │  │
    │  │   (Goal Decomposition, Task Scheduling)        │  │
    │  └───────────────┬───────────────────┬───────────┘  │
    │                  │                   │              │
    │  ┌───────────────▼─────┐   ┌─────────▼─────────┐  │
    │  │      MEMORY         │   │       TOOLS        │  │
    │  │ (Vector DB / Cache) │   │ (Search / Code /  │  │
    │  │                     │   │  APIs / Calc)      │  │
    │  └───────────────┬─────┘   └─────────┬─────────┘  │
    │                  └───────────────┼───────┘       │
    │                                  ▼               │
    │  ┌─────────────────────────────────────────────┐ │
    │  │              LLM (The Brain)                │ │
    │  │   (Reasoning, Decision Making, Generation)  │ │
    │  └─────────────────────────────────────────────┘ │
    └─────────────────────────────────────────────────────┘
```

**实战案例：**
在开发“数据分析 Agent”初期，我们仅配备了 LLM 和 SQL 执行工具。由于缺乏“Memory”，当用户修正查询条件（如“把时间范围缩小到上个月”）时，Agent 无法记住之前的表结构信息，导致每次都要重新生成 Schema 询问，效率极低。引入记忆模块后，它会缓存之前的分析结果，直接在此基础上修改 SQL，交互轮次减少了 60%。

## 面试追问
1. **冲突解决**：当 LLM 的规划 与 Memory 中记录的历史经验不一致时，应该听谁的？如何设计冲突解决机制？
2. **性能权衡**：在工具调用非常频繁的场景下（如每秒调用多次 API），如何平衡 Planning 带来的推理延迟与响应速度？
3. **Multi-Agent 协作**：在多 Agent 系统中，每个 Agent 只需要部分能力（如有的只做规划，有的只做执行），这种“分而治之”的策略在全栈 Agent 中有何优缺点？

## 易错点
1. **过度依赖 LLM 规划**：认为 LLM 无所不能，一次性就能生成完美的 Plan。实际上，对于长程任务（如超过 10 步的操作），LLM 往往需要引入 Reflexion（反思）机制，或者由外部控制器进行分步校验。
2. **忽视 Memory 的幻觉**：从向量库检索回来的记忆未必完全准确，如果直接喂给 LLM 作为事实，可能会导致“二次幻觉”。需要在 Prompt 中明确告知 LLM“需验证检索到的信息准确性”。
