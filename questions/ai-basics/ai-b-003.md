---
id: ai-b-003
difficulty: L2
category: ai-basics
subcategory: 多模态
tags:
- IO
- IOC
feynman:
  essence: 用图文指令数据训练，让模型学会按人类意图看图对话。
  analogy: 教小孩既看图又听话，让他学会指着图回答你的问题。
  first_principle: 如何打通视觉感知与语言理解的隔阂，实现多模态交互？
  key_points:
  - 对齐视觉特征与语言空间
  - 两阶段训练：预对齐+指令微调
  - 激发LLM的对话与推理能力
follow_up:
- LLaVA 的 Projection Layer 用 MLP 还是 Q-Former?
- 如何构造高质量的视觉指令数据?
---

# 什么是视觉指令微调(Visual Instruction Tuning)?它为什么能让 VLM 具备对话能力

- **视觉指令微调** 是让视觉语言模型(VLM)学会遵循人类多模态指令的技术。

- **核心思想**
- 类比 LLM 的指令微调(text-only instruction tuning)，但在输入中混合图像和文本
- 目标：让模型理解'看图回答问题'的指令格式

- **LLaVA 的做法(最经典实现)**

**Step 1: 构造指令数据**
- 用 GPT-4/Claude 基于图像描述自动生成多模态指令
- 格式：[IMAGE] + "请描述这张图片中的异常情况" → 回答
- 包含：描述、推理、对话、详细描述 4 种任务

**Step 2: 两阶段训练**
- **Stage 1: 特征对齐预训练**
  - 冻结 LLM 和 Vision Encoder
  - 只训练 Projection Layer(MLP)
  - 目标：让视觉特征对齐到 LLM 的 embedding 空间
- **Stage 2: 端到端指令微调**
  - 冻结 Vision Encoder
  - 微调 LLM + Projection Layer
  - 目标：学会遵循多模态指令

- **为什么有效**
1. 投影层让视觉特征'翻译'成 LLM 能理解的'伪文本 token'
2. 指令微调让模型学会'看图 → 理解意图 → 生成回答'的模式
3. 利用 LLM 已有的推理和对话能力

- **关键改进**：LLaVA-1.5 增加了多轮对话数据和高分辨率支持。

- **实战案例**：在搭建医疗咨询助手时，如果仅使用通用的图文对数据（Caption数据），模型只能描述"这是一个皮肤红肿的图"，无法回答"这是否是带状疱疹？"。经过视觉指令微调后，模型能理解"诊断"这一指令意图，结合视觉特征和医学知识给出推理建议。

- **代码示例 (Python)**：构造 LLaVA 格式的指令数据
```python
def construct_conversation(item):
    # item: {"image_id": "123", "caption": "A black cat...", "conversations": [...]}
    return {
        "id": item["image_id"],
        "image": f"images/{item['image_id']}.jpg",
        "conversations": [
            {"from": "human", "value": "<image>\nWhat is in this picture?"},
            {"from": "gpt", "value": item["caption"]}
        ]
    }
```

### 补充细节
- **数据构造细节**：LLaVA 使用了“Text-Only GPT-4”来生成对话数据。流程是：人工标注一批图像的 Caption，将 Caption 发送给 GPT-4，让 GPT-4 生成多样化的指令，从而低成本构建了 158K 的多模态指令数据。
- **格式模板**：输入通常被格式化为对话形式：
  ```text
  USER: <image>\nWhat is in this picture?
  ASSISTANT: A cat sitting on the sofa.
  ```
  模型通过预测 ASSISTANT 后的文本进行训练。
- **科学指令微调**：LLaVA-Instruct 还强调科学推理问答的微调，以提升模型的逻辑能力。

### ASCII 流程图：LLaVA 两阶段训练流程
```text
+-----------------------------------------------------------+
|                     Stage 1: Pre-training                |
| (Goal: Feature Alignment)                                |
+-----------------------------------------------------------+
| Image -> [Frozen ViT] -> Visual Feats                     |
 Text  -> [Caption]   -> [Frozen LLM Embeddings]            |
      Visual Feats + Text --> [Trainable Linear/MLP] --> LLM |
                           (Only Update MLP)                |
+-----------------------------------------------------------+
                                ↓
+-----------------------------------------------------------+
|                   Stage 2: Instruction Tuning            |
| (Goal: Instruction Following)                            |
+-----------------------------------------------------------+
| Image -> [Frozen ViT] -> Visual Feats                     |
 Prompt (Instruct) -> [Trainable LLM Embeddings]           |
      Visual Feats + Prompt --> [Trainable LLM]             |
                           (Update LLM + MLP)                |
+-----------------------------------------------------------+
```
