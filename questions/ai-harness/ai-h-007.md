---
id: ai-h-007
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- GC
- 索引
- Elasticsearch
feynman:
  essence: 根据场景复杂度和性能需求做权衡。
  analogy: 像买车，简单的用代步车（原生API），载重用卡车（LlamaIndex），越野用四驱（LangGraph）。
  first_principle: 如何在开发效率、运行性能和代码可维护性之间取得平衡？
  key_points:
  - 简单任务首选原生API，性能最好
  - RAG任务优先LlamaIndex
  - 复杂编排和Agent选LangChain/LangGraph
  - 追求Prompt质量选DSPy
  - 企业级稳定选Haystack
follow_up:
- 什么时候不用框架?
- 框架会不会成为技术债?
---

# 如何选择LLM应用框架?LangChain vs LlamaIndex vs DSPy vs 原生API

- **选型决策矩阵:**

```
你的需求是什么?
├─ 简单API调用 → 原生API/OpenAI SDK
├─ RAG系统
│   ├─ 快速原型 → LlamaIndex
│   └─ 生产级 → Haystack/LangChain
├─ Agent
│   ├─ 单Agent工具调用 → OpenAI Assistants API
│   ├─ 自定义Agent循环 → LangChain
│   └─ 复杂状态管理 → LangGraph
├─ Prompt优化 → DSPy
├─ 结构化输出 → Instructor/Outlines
└─ 全栈应用 → LangChain生态
```

- **实战案例**：在一个金融合规项目初期，我们使用 LangChain 快速验证了 Agent 思路；但在上线前，由于 LangChain 的稳定性问题和链路追踪困难，我们将核心检索逻辑迁移到 LlamaIndex 以利用其高级查询优化，同时将整体编排迁移到 Haystack 以满足企业级日志和监控要求。

- **详细对比:**

| 维度 | 原生API | LangChain | LlamaIndex | DSPy | Haystack |
|------|---------|-----------|------------|------|----------|
| 学习成本 | **最低** | 高 | 中 | 高 | 中 |
| 灵活性 | **最高** | 中 | 中 | 中 | 中 |
| 抽象层级 | 无 | 高 | 中 | 高 | 高 |
| RAG能力 | 需自建 | 强 | **最强** | 弱 | 强 |
| Agent能力 | 需自建 | **强** | 中 | 弱 | 中 |
| Prompt优化 | 需自建 | 弱 | 弱 | **最强** | 弱 |
| 生产就绪 | **是** | 中 | 中 | 低 | **高** |

- **个人建议:**
- 快速验证:原生API + 几个辅助函数
- 中等复杂度:LangChain
- RAG为主:LlamaIndex
- 追求效果:DSPy
- 追求稳定:Haystack

**## 边界情况**
1. **多模型混合调用**：在一个 Pipeline 中同时调用 OpenAI（用于推理）和本地开源模型（用于低成本摘要）时，选择框架需考虑其对异构模型路由的支持能力（LangChain 和 Haystack 支持较好）。
2. **极低延迟要求**：在需要毫秒级响应的场景（如高频交易信号判断），原生 API + 手写优化代码通常是唯一选择，任何框架的抽象层都会带来不可忽视的延迟开销。

**## 面试追问**
1. 如果团队技术栈以 Java/Go 为主，Python 的 LangChain 生态是否还适用？有没有跨语言的对标方案？
2. 如何评估引入一个重框架（如 LangChain）带来的“黑盒”风险？有没有具体的观测或调试手段？
3. 在项目从 POC 转向生产的过程中，代码重构的成本通常在哪个阶段最高？如何通过架构设计降低这种成本？

**## 易错点**
1. **过度抽象**：认为“框架越强大越好”。实际上，对于简单逻辑，引入 LangChain 可能导致代码变得难以阅读和调试（Magic 过多）。
2. **忽视框架的版本迭代**：LLM 框架迭代极快（如 LangChain 几周一个 Breaking Change）。选型时需评估社区活跃度与稳定性，避免引入停止维护或频繁变动的库。

---

## 常见考点
1. **Vendor Lock-in（厂商锁定）**：如果过度依赖 LangChain/LangGraph 等重框架，未来迁移底层模型（如从 OpenAI 切到 Llama 3）或框架本身的难度如何？回答：抽象层越高，切换成本通常越低（只需更换底层 LLM Runnable），但框架本身的 API 变动（如 LangChain v0.1 -> v0.2 迁移）可能带来维护成本。
2. **原生 API 的适用边界**：所谓「简单 API 调用」具体指什么？通常指单轮对话、简单的 System Prompt + User Prompt 组合。一旦涉及 Prompt Template 管理、多轮对话历史维护、工具调用等，手写原生 API 的代码量会指数级上升，且难以维护。
3. **框架的「黑盒」风险**：使用 LangChain 或 LlamaIndex 时，开发者可能不清楚底层的 Prompt 拼接逻辑或 Token 计算方式。面试时强调：生产环境需要深入阅读源码或使用 LangSmith 等观测工具，避免因为框架的默认行为导致不可控的成本或效果问题。
4. **DSPy 的冷启动问题**：DSPy 需要训练集来优化 Prompt。在冷启动（没有数据）阶段，如何选择？通常先用 LangChain 或原生 API 快速跑通流程，收集日志数据后，再转用 DSPy 进行效果优化。
