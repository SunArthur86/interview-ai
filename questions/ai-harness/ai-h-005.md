---
id: ai-h-005
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- IO
- GC
- IOC
- Elasticsearch
feynman:
  essence: 给LLM应用引入状态机与循环控制。
  analogy: 从简单的流水线变成了可以回退、分支的电路图，支持复杂的决策循环。
  first_principle: 如何为拥有记忆和循环逻辑的Agent提供控制流？
  key_points:
  - 基于图的状态管理，支持Cyclic（循环）结构
  - 解决LCEL无法处理循环和全局状态的问题
  - 支持Human-in-the-Loop（人工介入）
  - 适用于复杂Agent和多步推理场景
follow_up:
- LangGraph的Checkpointer如何工作?
- 如何实现Human-in-the-Loop?
---

# LangGraph解决了LangChain的什么问题?什么时候应该用LangGraph

- **LangChain LCEL的局限:**

LCEL是DAG(有向无环图),不支持:
1. **循环** - 无法实现「执行→检查→重试」循环
2. **条件分支** - 只能线性管道
3. **状态管理** - 无全局状态
4. **中断/恢复** - 无法暂停

- **实战案例**：构建代码审查 Agent 时，必须使用 LangGraph 实现“Linter 生成修复建议 -> 单元测试 -> (测试失败?) -> 回退到 Linter 重试”的闭环，LCEL 无法处理这种带状态反馈的循环。

- **LangGraph解决方案:StateGraph**

```python
# 定义全局状态
class State(TypedDict):
    messages: list
    current_node: str
    retry_count: int

# 构建图
graph = StateGraph(State)
graph.add_node("agent", agent_node)
graph.add_node("tools", tool_node)
graph.add_edge(START, "agent")
graph.add_conditional_edges("agent", should_continue,
{"continue": "tools", "end": END})
graph.add_edge("tools", "agent")  # 循环!

app = graph.compile(checkpointer=MemorySaver())
```

**架构示意图:**
```
      (Start)
        |
        v
  +---------+      (条件判断)
  | Agent   | <------------------+
  +---------+                     |
        |                        |
   continue?                      |
        | Yes                     |
        v                        |
  +---------+                     |
  |  Tools  | ---------------------+
  +---------+      (Action Done)
```

- **什么时候用LangGraph:**
1. 需要循环(Agent重试/反思/多轮)
2. 需要条件分支(路由/审批)
3. 需要全局状态管理
4. 需要中断/恢复
5. 多Agent协作编排

- **LangGraph vs LangChain LCEL 对比:**

| 特性 | LangChain LCEL | LangGraph |
|------|----------------|-----------|
| 流程控制 | 线性 DAG | **Cyclic Graph (循环/分支)** |
| 状态管理 | 隐式 | **显式 StateGraph** |
| 人机协作 | 较弱 | **强大的中断/恢复支持** |
| 适用场景 | 简单链式任务 | **复杂 Agent 编排** |

- **什么时候不用:**
- 简单的prompt→model→parser → 用LCEL
- 无状态的API调用 → 用普通函数

---

## 常见考点
1. **State 的传递机制**：LangGraph 中的每个 Node 都接收完整的 State 对象，并返回 State 的更新部分（Partial Update）。Reducer 函数（如 `add_messages`）如何处理状态更新冲突（如多个节点同时更新 messages 列表）？
2. **CheckpointSaver 的作用**：`MemorySaver` 如何实现 Human-in-the-Loop？它将每一步的状态快照持久化（存到 Redis 或内存），允许用户通过 `thread_id` 恢复之前的对话状态并在特定节点暂停。
3. **Node 的输入输出约定**：Node 函数的签名通常是什么？`(state: State) -> Dict[str, Any]`。返回的字典会自动合并到全局 State 中。
4. **LangGraph vs State Machine**：LangGraph 本质上是一个 Pregel-inspired 的状态机实现。与传统的 FSM（有限状态机）相比，它的优势在于支持多 Agent 并行执行和复杂的消息传递协议。
