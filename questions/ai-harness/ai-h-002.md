---
id: ai-h-002
difficulty: L2
category: ai-harness
subcategory: LLM框架
tags:
- IO
- GC
- 索引
- Elasticsearch
feynman:
  essence: LangChain管手脚，LlamaIndex管消化。
  analogy: 像厨师，LangChain是工作流控制，LlamaIndex是专门处理食材（数据）的菜刀。
  first_principle: 如何解决LLM应用中数据获取与逻辑调用的分工问题？
  key_points:
  - LangChain侧重通用逻辑编排与Agent开发
  - LlamaIndex侧重数据索引、检索与RAG优化
  - 核心抽象差异：Chain vs Node/Index
  - 纯RAG选LlamaIndex，复杂流程选LangChain
follow_up:
- LlamaIndex的Router Query如何工作?
- 两者能否混用?
memory_points:
- 定位口诀：LangChain管手脚(逻辑编排)，LlamaIndex管消化(数据)。
- 擅长差异：LangChain做Agent/链式/工具，LlamaIndex做RAG/索引/查询。
- 抽象差异：Runnable/Chain vs Document/Node/Index。
- 选择：纯RAG选LlamaIndex，Agent/工作流选LangChain/LangGraph。
- 易错点：Node过细则上下文断裂；Index是存储，Retriever才检索。
---

# LlamaIndex和LangChain的定位有什么区别?各自擅长什么场景

- **定位差异:**

| | LangChain | LlamaIndex |
|--|-----------|------------|
| 核心定位 | **通用LLM应用框架** | **数据+LLM框架** |
| 擅长 | Agent/链式调用/工具 | **RAG/索引/查询** |
| 抽象 | Runnable/Chain | **Document/Node/Index** |
| 数据处理 | 基础 | **深度优化** |
| 查询引擎 | 基础 | **多种查询模式** |

- **LlamaIndex核心概念:**
1. **Document** - 原始文档(PDF/HTML/Markdown)
2. **Node** - 文档分块后的最小单元
3. **Index** - 节点的索引结构(向量/树/关键词)
4. **Query Engine** - 查询引擎(路由/子问题/多步)
5. **Retriever** - 检索器

- **LlamaIndex高级查询模式:**
- **Sub-Question Query** - 拆分复杂问题
- **Multi-Document** - 跨文档推理
- **Recursive Retrieval** - 分层检索
- **Router Query** - 动态选择检索策略

- **选择建议:**
- 纯RAG → LlamaIndex(数据处理更细)
- Agent/工作流 → LangChain/LangGraph
- 通用 → 两者结合

**边界情况补充**
1. **数据更新延迟**：LlamaIndex 的索引默认是静态的。若源数据频繁更新（如数据库实时写入），必须重新构建索引或使用特定的 Vector Store（如支持 upsert 的），否则会导致查询结果陈旧。
2. **多模态数据处理**：LlamaIndex 支持多模态 RAG，但 LangChain 在多模态非结构化数据的处理流程上相对薄弱。如果涉及图片/表格混合检索，LlamaIndex 的 Node 结构和 MultiModal 模型集成更具优势。

**实战案例**
在处理百万级 PDF 文档库时，直接用 LangChain 的基础向量检索导致准确率极低（大量跨章节上下文丢失）。改用 LlamaIndex 的 `Recursive Retrieval`（先检索摘要块，再回溯原文块）后，召回相关度从 0.6 提升至 0.85。

**代码示例（LlamaIndex QueryEngine）**
```python
from llama_index.core import VectorStoreIndex, Document
from llama_index.core.query_engine import RouterQueryEngine
from llama_index.core.selectors import LLMMultiSelector

# 创建索引
documents = [Document(text="...")]
index = VectorStoreIndex.from_documents(documents)

# 配置查询引擎（支持混合检索）
query_engine = index.as_query_engine(
    similarity_top_k=5,
    response_mode="tree_summarize"  # 针对大量上下文的总结模式
)

response = query_engine.query("项目的核心风险是什么？")
```

## 常见考点
1. **Nodes vs Documents**：为什么 LlamaIndex 引入 Node 概念？因为 Vector Store 通常针对固定大小的 Chunk 进行检索，而 Document 可能非常长。Node 允许携带更丰富的元数据（如父文档引用、关系），实现「检索到小节点，返回大上下文」。
2. **Recursive Retrieval 原理**：如何实现从粗到细的检索？通常先检索摘要或元数据，再根据结果检索对应的具体块，常用于处理大规模文档库。
3. **LangChain 与 LlamaIndex 的互操作性**：能否在 LangChain 中使用 LlamaIndex 的 Retriever？可以，LlamaIndex 提供了 `LangChainRetriever` 包装器，反之亦然。
4. **Post-processing（后处理）**：LlamaIndex 在检索后、生成前支持哪些操作？如 Rerank（重排序）、Filters（元数据过滤）和 Fusion（多路查询结果合并）。

## 易错点
1. **过度索引与检索粒度**：在使用 LlamaIndex 时，错误地将 Document 切分得过细（如每 50 tokens 一个 Node），虽然检索匹配度高，但传入 LLM 的上下文缺乏连贯性，导致生成质量下降。需注意 `chunk_size` 的权衡。
2. **混淆 Index 与 Retriever**：LlamaIndex 中 Index 是数据的存储结构，而实际执行检索逻辑的是 Retriever。很多人误以为必须通过 `Index.as_retriever()` 才能检索，实际上可以直接组合自定义 Retriever。

## 面试追问
1. 在 RAG 系统中，LlamaIndex 的 `Recursive Retrieval` 相比于简单的向量检索，具体解决了什么问题？它的实现开销主要在哪里？（引导：解决上下文截断问题；开销在于多跳查询和元数据维护）
2. LangChain 正在大力发展 LangGraph 进行状态管理，而 LlamaIndex 也有 Workflows/Agents 功能。在构建一个“读取文档并执行代码”的 Agent 时，你会选择哪个框架作为主体？为什么？（引导：涉及代码执行和复杂状态流转选 LangChain；涉及复杂文档解析选 LlamaIndex）
3. 如何评价 LlamaIndex 的“知识图谱索引”在实际生产中的可用性？相比 Neo4j 原生方案有什么优劣？

## 记忆要点

- 定位口诀：LangChain管手脚(逻辑编排)，LlamaIndex管消化(数据)。
- 擅长差异：LangChain做Agent/链式/工具，LlamaIndex做RAG/索引/查询。
- 抽象差异：Runnable/Chain vs Document/Node/Index。
- 选择：纯RAG选LlamaIndex，Agent/工作流选LangChain/LangGraph。
- 易错点：Node过细则上下文断裂；Index是存储，Retriever才检索。
